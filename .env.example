# ── PostgreSQL (Neon) ─────────────────────────────────
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# ── Anthropic (Claude) ────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── GoHighLevel ───────────────────────────────────────
GHL_API_KEY=your-ghl-api-key
GHL_LOCATION_ID=your-location-id

# ── Security ──────────────────────────────────────────
# Secret token GHL sends in the webhook header so we know it's really GHL
GHL_WEBHOOK_SECRET=make-up-a-random-string-here

# ── Newsletter settings ───────────────────────────────
# Comma-separated topics for Claude to cover each day
NEWSLETTER_TOPICS=dollar devaluation,BRICS de-dollarization,gold and silver markets,US debt crisis,financial sovereignty,Fed monetary policy,inflation wealth protection

# Send time: cron format  (default = 8:00 AM UTC)
CRON_SCHEDULE=0 8 * * *

# ── Phase 2: Content aggregator & tiered newsletters ──
# GHL tag used to send the free teaser newsletter
FREE_LEADS_TAG=newsletter-free-leads
# Optional: OpenAI API key for future DALL-E image generation
OPENAI_API_KEY=

# ── Server ────────────────────────────────────────────
PORT=3000
